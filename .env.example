# Copy to ".env" in the project root (optional). Values override defaults only
# when not already set in the process environment.

# IRAAP_API_HOST=0.0.0.0
# IRAAP_API_PORT=8000
# IRAAP_WS_PORT=8765

# Optional: path to YAML stack profile (see config/default_iraap.yaml)
# IRAAP_PROFILE=/path/to/my_iraap.yaml
